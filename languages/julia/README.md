# AgentBible Julia

Julia package for the AgentBible v1 correctness specification.

## Test

```julia
using Pkg
Pkg.test()
```

The package provides both function forms like `check_unitary` and macro forms
like `@validate_unitary`.
