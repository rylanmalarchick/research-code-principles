# agent_docs

Historical planning notes from the pre-v1 repository transformation. These
files are retained only as archive material and are not authoritative. Use
`AGENTS.md`, `SPEC.md`, and the current code/tests instead.
